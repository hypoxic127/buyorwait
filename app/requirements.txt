# Pinned exactly: this file is what Cloud Run builds from, and the app depends on
# Streamlit 1.58 APIs (st.navigation, st.pills, st.metric(border=/chart_data=),
# st.container(height="stretch")). With >= bounds a rebuild months later silently
# pulls a different stack than the one that was tested. These are the versions the
# app was verified against in the browser.
streamlit==1.58.0
plotly==6.9.0
google-cloud-bigquery==3.42.1
db-dtypes==1.7.0
pandas==3.0.3
pyarrow==24.0.0
google-genai==2.10.0
requests==2.34.2
fastembed==0.8.0
