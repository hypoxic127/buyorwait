# Deliberately NOT pinned like app/requirements.txt. pipeline.py has to import cleanly
# under `python -m cudf.pandas`, and the RAPIDS conda environment pins numpy/pandas/
# pyarrow itself — hard-pinning them here would fight the GPU stack it has to run on.
# Lower bounds only; the CPU baseline and the GPU run must resolve to the same
# versions for the benchmark to be a fair comparison.
numpy>=1.26
pandas>=2.1
pyarrow>=15
requests>=2.31
google-cloud-bigquery>=3.20
db-dtypes>=1.2
